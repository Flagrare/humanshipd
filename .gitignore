# Assistant working files (local memory/logs, not project content)
.remember/

# Rust build artifacts (Cargo.lock is committed — workspace has binaries)
/target/
web-verify/target/
**/*.rs.bk

# WASM build output (regenerate with `wasm-pack build`)
web-verify/pkg/

# Node / Playwright (extension tests)
node_modules/
test-results/
playwright-report/
.playwright-mcp/

# OS / editor cruft
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/
